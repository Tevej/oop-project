package main.se.tevej.game.controller.input.base;

/**
 * A specification for our classes which listens for when the user moves the mouse.
 */
public interface OnMovedListener {
    void onMoved(TMouse mouse);
}
